<div class="countriesmainh">Select Your Country</div>
<div style="width:100%;">
															<div class="countriesmainscreen">
															<ul class="mysqueeb">
																 <?php foreach($countries as $country): ?>
																 <?php if($country->id<=ceil($countries->last()->id/6)): ?>
																
																	<li>
																		<strong>
																 		 <a href="<?php echo URL::route('selectcountryid',strtolower($country->code)); ?>"><?php echo $country->alias; ?></a>
																 		</strong>
																   </li>
																
																<?php endif; ?>
																<?php endforeach; ?>
															</ul>
															</div>
															
															<div class="countriesmainscreen">
															<ul class="mysqueeb">
																 <?php foreach($countries as $country): ?>
																 <?php if($country->id>ceil($countries->last()->id/6) && $country->id<=ceil($countries->last()->id/6)*2): ?>
																
																	<li>
																		<strong>
																 		 <a href="<?php echo URL::route('selectcountryid',strtolower($country->code)); ?>" ><?php echo $country->alias; ?></a>
																 		</strong>
																   </li>
																
																<?php endif; ?>
																<?php endforeach; ?>
															</ul>
															</div> 
															
															
															<div class="countriesmainscreen" >
															<ul class="mysqueeb">
																 <?php foreach($countries as $country): ?>
																 <?php if($country->id>ceil($countries->last()->id/6)*2 && $country->id<=ceil($countries->last()->id/6)*3): ?>
																
																	<li>
																		<strong>
																 		 <a href="<?php echo URL::route('selectcountryid',strtolower($country->code)); ?>" ><?php echo $country->alias; ?></a>
																 		</strong>
																   </li>
																
																<?php endif; ?>
																<?php endforeach; ?>
															</ul>
															</div> 
															
															<div class="countriesmainscreen">
															<ul class="mysqueeb">
																 <?php foreach($countries as $country): ?>
																 <?php if($country->id>ceil($countries->last()->id/6)*3 && $country->id<=ceil($countries->last()->id/6)*4): ?>
																
																	<li>
																		<strong>
																 		 <a href="<?php echo URL::route('selectcountryid',strtolower($country->code)); ?>" ><?php echo $country->alias; ?></a>
																 		</strong>
																   </li>
																
																<?php endif; ?>
																<?php endforeach; ?>
															</ul>
															</div> 
															
															<div class="countriesmainscreen">
															<ul class="mysqueeb">
																 <?php foreach($countries as $country): ?>
																 <?php if($country->id>ceil($countries->last()->id/6)*4 && $country->id<=ceil($countries->last()->id/6)*5): ?>
																
																	<li>
																		<strong>
																 		 <a href="<?php echo URL::route('selectcountryid',strtolower($country->id)); ?>"  ><?php echo $country->alias; ?></a>
																 		</strong>
																   </li>
																
																<?php endif; ?>
																<?php endforeach; ?>
															</ul>
															</div> 
															
															<div class="countriesmainscreen">
															<ul class="mysqueeb">
																 <?php foreach($countries as $country): ?>
																 <?php if($country->id>ceil($countries->last()->id/6)*5): ?>
																
																	<li>
																		<strong>
																 		 <a href="<?php echo URL::route('selectcountryid',strtolower($country->code)); ?>" ><?php echo $country->alias; ?></a>
																 		</strong>
																   </li>
																
																<?php endif; ?>
																<?php endforeach; ?>
															</ul>
															</div> 
															
														</div>
				